name: Backend CI
on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  store-secret:
    runs-on: ubuntu-latest
    env:
      SSH_OC_IP: ${{ secrets.SSH_OC_IP }}
    # SSH_USER: ${{ secrets.SSH_USER }}
    # SSH_STORE_KEY: ${{ secrets.SSH_STORE_KEY }}
    # SSH_PORT: ${{ secrets.SSH_PORT }}
    # WORK_DIR: ${{ secrets.WORK_DIR }}
    steps:
      - name: 'see secrets'
        run: |
          echo "shh oc ip: ${{ env.SSH_OC_IP }}"
      - name: 'general secrets'
        run: |
          echo "data: ${{ toJSON(github.event) }}"
      # example-command "$SUPER_SECRET"
      # - name: executing remote ssh commands using ssh key
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ env.SSH_OC_IP }}
      #     username: ${{ env.SSH_USER }}
      #     key: ${{ env.SSH_STORE_KEY }}
      #     port: ${{ env.SSH_PORT }}
      #     script: |
      #       cd ${{ env.WORK_DIR }}
      #       [ ! -d "${{ env.WORK_DIR }}/store-backend" ] && git clone https://github.com/an-tet/store-backend.git
      #       cd store-backend
      #       git pull
